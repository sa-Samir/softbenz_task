import 'package:flutter/material.dart';

class FlashSaleProductScreen extends StatelessWidget {
  const FlashSaleProductScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
      body: const Center(
        child: Text('This is the flash sale products page.'),
      ),
    );
  }
}
